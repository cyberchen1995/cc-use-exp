---
applyTo: "**/*.vue,**/*.tsx,**/*.ts,**/*.jsx,**/*.js,**/*.css,**/*.scss"
---

前端相关任务遵守以下约定：

- 优先保持现有组件边界、状态流和样式组织方式。
- 不机械添加无证据的 fallback、try/catch 或额外抽象层。
- 发现列表、分页、筛选或详情异常时，先核对接口输入输出契约和统一响应格式，再决定是否保留临时兼容。
- 不用临时兼容长期掩盖后端接口结构漂移。
