---
applyTo: "**"
---

在本仓库中工作时：

- 先阅读相关代码、配置和脚本，再下结论。
- 优先做能解决问题的最小改动。
- 优先复用现有模式，再考虑新增抽象。
- 不主动扩需求；只有在安全完成当前任务必须时才扩大改动范围。
- 默认使用简体中文回复；代码、命令、日志、报错和协议字段保持原文。
- 文档和代码中的作者署名统一使用 `wwj`。
- 如果生成 Java 新文件，默认补充包含 `@author wwj` 的文件头注释；修改已有 Java 文件时，仅在任务已触达且符合现有注释风格时处理。
