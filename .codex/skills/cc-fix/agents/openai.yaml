interface:
  display_name: "fix"
  short_description: "显式 bug 修复与结构化 debug 工作流。"
  default_prompt: "Use $fix to confirm the expected behavior and current symptom before reading the relevant code and proposing a minimal fix."
policy:
  allow_implicit_invocation: false
