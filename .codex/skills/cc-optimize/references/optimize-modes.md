# Optimize Modes

模式与默认维度：

- `full`: UX + PERF + CODE + SUGAR + 基础安全卫生 + BEST
- `ux`: 表单体验、加载反馈、错误提示、空状态、操作费力度
- `perf`: 查询路径、分页、N+1、缓存、中间件、构建与包体热点
- `code`: 重复逻辑、大文件、死代码、硬编码、现代语法替代

使用原则：

- `full` 适合阶段性体检，不适合每次都跑
- `ux` 面向用户感知问题
- `perf` 面向吞吐、等待时间和中间件引入时机
- `code` 面向维护成本，不要把“风格不同”误判成问题
