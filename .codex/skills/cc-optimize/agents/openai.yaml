interface:
  display_name: "optimize"
  short_description: "显式优化扫描，支持 full、ux、perf、code 四种模式。"
  default_prompt: "使用 $optimize 时，先界定扫描范围，再产出按影响面排序、有证据支撑的报告，而不是直接动手改代码。"
policy:
  allow_implicit_invocation: false
