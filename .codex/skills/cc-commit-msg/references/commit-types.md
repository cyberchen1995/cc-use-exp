# Commit Types

按“改动意图”而不是按文件后缀选类型：

- `feat`: 新增用户可感知功能
- `fix`: 修复错误行为、回归或缺陷
- `refactor`: 不改变外部行为的结构调整
- `style`: 纯格式或排版调整，不影响逻辑
- `docs`: 文档、注释、说明更新
- `test`: 新增或调整测试
- `chore`: 构建、依赖、配置、脚手架或杂务

若一批改动同时包含多个意图：

- 先选主意图
- 若没有明显主意图，提醒用户拆分提交
- 不要为了凑类型而把混杂改动硬写成 `feat`
